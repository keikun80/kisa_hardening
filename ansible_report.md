# Ansible 도입 분석 보고서: 장단점, 보안 취약점 및 극복 방안

본 보고서는 인프라 자동화 및 구성 관리 도구인 **Ansible**을 도입할 때 고려해야 할 핵심 요소를 다룹니다. 도입에 따른 장단점을 비교 분석하고, 발생할 수 있는 보안 취약점과 실무에서 적용할 수 있는 구체적인 극복 방안을 제안합니다.

---

## 1. Ansible 개요
Ansible은 Red Hat에서 스폰서하는 오픈소스 IT 자동화 엔진으로, **구성 관리(Configuration Management)**, **애플리케이션 배포**, **프로비저닝** 등을 자동화합니다. 
대상 서버에 별도의 에이전트를 설치하지 않는 **에이전트리스(Agentless)** 방식과 YAML 형식의 **플레이북(Playbook)**을 사용하는 선언적 접근 방식이 가장 큰 특징입니다.

---

## 2. Ansible 도입의 장단점

### 2.1. 장점 (Pros)
* **에이전트리스(Agentless) 아키텍처**
  * 대상 노드(Target Node)에 관리용 에이전트나 데몬을 설치할 필요가 없습니다. SSH(Linux/Unix)나 WinRM(Windows) 인프라만 있으면 즉시 연동되므로 리소스 소모가 적고 초기 도입 비용이 낮습니다.
* **낮은 학습 곡선 및 높은 가독성**
  * 플레이북이 인간이 읽기 쉬운 **YAML** 포맷으로 작성되므로, 개발자와 인프라 엔지니어 간의 협업(DevOps)이 용이하며 인프라 상태를 코드로 문서화(IaC)하기 편리합니다.
* **멱등성(Idempotency) 보장**
  * 동일한 플레이북을 여러 번 실행하더라도, 대상 시스템의 상태가 이미 원하는 상태(Desired State)에 도달해 있다면 변경 작업을 중복 수행하지 않습니다. 이로 인해 운영의 안정성과 예측 가능성이 대폭 향상됩니다.
* **풍부한 에코시스템 및 모듈**
  * 클라우드 리소스(AWS, Azure, GCP), 네트워크 장비, 운영체제 설정, 컨테이너 등을 제어할 수 있는 수천 개의 사전 정의 모듈이 기본 제공됩니다.
* **유연한 확장성**
  * 간단한 일회성(Ad-hoc) 명령어 실행부터 복잡한 다중 티어 애플리케이션 배포 및 롤링 업데이트까지 커버 가능합니다.

### 2.2. 단점 및 한계 (Cons)
* **실시간 실행 속도 및 성능 오버헤드 (Push 방식의 한계)**
  * 중앙 제어 노드(Control Node)에서 대상 노드들로 SSH 연결을 순차적/병렬로 맺고 파이썬 스크립트를 전송하여 실행하는 **Push 방식**을 사용합니다. 대상 노드 수가 수천 대 이상으로 늘어나면 네트워크 오버헤드와 CPU 부하로 인해 Chef나 Puppet 같은 Agent-Pull 방식에 비해 실행 속도가 느려질 수 있습니다.
* **상태 관리(State Management)의 부재**
  * Terraform과 같이 인프라 리소스의 현재 상태를 유지하는 별도의 `State File` 개념이 없습니다. 이 때문에 플레이북에서 특정 리소스 생성을 삭제하더라도, 실제 인프라에서 기존 리소스가 자동으로 지워지지 않아 잔적 리소스 관리가 수동으로 이루어질 수 있습니다.
* **복잡한 제어 로직 구현의 가독성 저하**
  * 절차 지향적인 복잡한 로직(다중 반복문, 예외 처리, 조건부 에러 분기 등)을 YAML의 선언적 문법으로 구현하려고 하면 코드가 매우 지저분해지고 유지보수가 어려워집니다.
* **윈도우(Windows) 환경 관리의 번잡함**
  * 리눅스에 비해 Windows 제어를 위한 WinRM(Windows Remote Management) 설정 및 인증서 구성이 상대적으로 복잡하며, 리눅스 전용 모듈에 비해 윈도우 지원 모듈의 다양성이 다소 부족합니다.

---

## 3. Ansible 보안상 취약점

Ansible은 중앙 집중식 제어 구조를 가지므로, 제어 노드의 보안이 뚫리거나 잘못된 설정이 공유될 경우 전체 인프라가 마비되는 위험을 내포하고 있습니다.

| 취약점 영역 | 주요 위험 내용 |
| :--- | :--- |
| **1. 자격 증명 노출 (Credential Exposure)** | 플레이북 코드나 변수 설정 파일(YAML) 내에 데이터베이스 패스워드, SSH 키, API Token 등의 민감 정보가 평문(Plain Text)으로 기재되어 Git 저장소에 노출되는 사고. |
| **2. 통신 채널 보안 취약 (Transport Layer)** | 컨트롤러와 노드 간 SSH/WinRM 통신 시 취약한 암호화 알고리즘 사용, SSH Host Key Verification 비활성화 설정(`host_key_checking = False`)으로 인한 중간자 공격(MITM) 노출. |
| **3. 권한 상승 남용 (Privilege Escalation)** | 플레이북에서 `become: yes` 설정을 남용하고, 대상 대상 노드의 `sudo` 권한을 비밀번호 없이 통제되지 않는 범위(`ALL=(ALL) NOPASSWD: ALL`)로 열어두어 공격자에게 Root 권한 탈취 빌미를 제공. |
| **4. 검증되지 않은 외부 소스 코드 실행** | Ansible Galaxy 등 신뢰성이 완벽히 검증되지 않은 오픈소스 Roles 및 Collections을 가져와 검증 없이 실행하다가 악성 스크립트나 백도어가 유입되는 경우. |
| **5. 제어 노드(Control Node) 단일 장애점** | 모든 대상 노드로 통하는 SSH 프라이빗 키와 권한이 집중되어 있는 Ansible 컨트롤러가 탈취당할 경우, 단 한 번의 해킹으로 전체 서버 인프라가 완전 장악당하는 치명적인 결과 초래. |

---

## 4. 보안 취약점 극복 방안

보안 위협을 해결하고 안전하게 Ansible을 도입하기 위해서는 기술적 조치와 운영 정책 수립이 병행되어야 합니다.

### 4.1. 민감 정보 암호화 및 비밀 관리 (Secrets Management)
* **Ansible Vault 도입**
  * 암호화가 필요한 패스워드나 개인키 파일은 `ansible-vault`를 사용하여 AES-256 알고리즘으로 대칭 암호화합니다. Git 등 형상 관리 시스템에는 암호화된 파일만 업로드하고, 실행 시에 복호화 비밀번호를 주입받아 사용합니다.
* **외부 비밀정보 저장소(Secrets Manager) 연동**
  * 하드코딩된 변수 대신 HashiCorp Vault, AWS Secrets Manager, CyberArk 등의 전문 자격증명 솔루션과 연동(Ansible lookup 플러그인 활용)하여, 런타임에 일회성/동적 토큰 및 패스워드를 불러와 실행하도록 구성합니다.

### 4.2. SSH 및 WinRM 채널 보안 강화
* **호스트 키 검증(Host Key Checking) 유지**
  * `ansible.cfg`에서 `host_key_checking = True` 설정을 엄격하게 유지하여, 알려지지 않은 호스트와의 비정상 통신을 차단하고 MITM 공격을 원천 봉쇄합니다.
* **통신 전용 계정 및 프로토콜 최적화**
  * 통신용 SSH 키 생성 시 RSA 4096비트 또는 Ed25519와 같은 강력한 알고리즘을 사용하고 SSH Key에 암호를 지정합니다.
  * Windows 노드 관리 시 HTTP 프로토콜을 사용한 WinRM을 전면 차단하고, HTTPS 또는 Kerberos 인증 기반의 보안 전송 채널을 강제합니다.

### 4.3. 권한 최소화 원칙 (Principle of Least Privilege) 적용
* **정교한 sudoers 설정**
  * Ansible 실행용 시스템 계정(예: `ansible_user`)을 생성하고, 이 계정이 실행해야 하는 관리용 명령어 세트만 선별하여 `sudo` 권한을 제한 부여합니다.
* **`become` 옵션 타겟팅**
  * 플레이북 전체나 호스트 레벨에서 무조건 루트 권한으로 실행하게 만드는 대신, 반드시 Root 권한이 필요한 개별 태스크(Task) 단위로만 `become: yes`와 `become_user`를 세분화하여 선언합니다.

### 4.4. 품질 검증 및 컴플라이언스 체계 구축
* **정적 분석 도구(`ansible-lint`)의 CI/CD 파이프라인 연동**
  * 코드가 리포지토리에 푸시될 때 `ansible-lint`를 통해 하드코딩된 패스워드 검출, 낡은 모듈 사용 여부, 권한 상승 남용 등의 안티패턴을 자동으로 검사하고 차단합니다.
* **로컬 미러링 및 검증 코드 구성**
  * Ansible Galaxy에서 가져오는 외부 Role은 즉시 배포하지 않고, 안전성이 확인된 특정 버전만 로컬 아티팩토리(Artifactory)에 저장하여 참조하는 방식을 사용합니다.

### 4.5. 감사(Audit) 및 통제 환경 마련
* **사용자 및 실행 기록 감사 로그 중앙 집중화**
  * Ansible의 실행 로그(`ansible.log`) 및 대상 호스트의 시스템 로그(`/var/log/secure` 등)를 중앙 SIEM(예: Splunk, ELK Stack)으로 실시간 전송하여, '누가, 언제, 어떤 노드에서, 어떤 플레이북을 실행했는지'에 대한 추적성을 확보합니다.
* **Ansible AWX / Automation Controller 도입**
  * CLI 환경 대신 AWX(오픈소스) 또는 Red Hat Ansible Automation Controller를 구축하여 웹 UI 기반의 역할 기반 접근 제어(RBAC), 중앙 자격 증명 관리, 승인 워크플로우 등을 결합하여 원격 실행 권한을 엄격히 감시합니다.
