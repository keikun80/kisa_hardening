# AWS 기반 SIEM(보안 정보 및 이벤트 관리) 서비스 추천 및 비교 분석

AWS 환경에서 보안 위협 탐지, 컴플라이언스 준수 및 로그 통합 분석을 위해 **SIEM(Security Information and Event Management)** 시스템을 구축할 때 활용하기 좋은 AWS 서비스와 아키텍처 조합을 추천합니다.

AWS는 단일 완성형 SIEM 제품을 공급하기보다, **사용자의 요구사항(인프라 규모, 예산, 관리 공수, 연동 필요성)**에 맞춰 조합할 수 있는 여러 서비스와 공식 솔루션을 제공합니다.

---

## 1. 추천 AWS 서비스 및 솔루션 탑 4

### ① SIEM on Amazon OpenSearch Service (가장 추천)
> **"AWS가 공식 제공하는 오픈소스 기반 패키지형 SIEM 솔루션"**

Amazon OpenSearch Service(구 Elasticsearch)를 백엔드로 사용하며, AWS가 미리 최적화해 둔 CloudFormation 템플릿과 통합 스크립트를 제공하는 세미 네이티브(Semi-native) SIEM 구축 방식입니다.

* **동작 방식**: 각 AWS 서비스(CloudTrail, VPC Flow Logs, GuardDuty, AWS WAF 등)의 로그를 Amazon S3로 수집한 후, AWS Lambda가 이를 정규화(JSON 변환 및 파싱)하여 OpenSearch로 전송하고 시각화합니다.
* **장점**:
  * **사전 정의된 대시보드**: 로그인 시도 실패, 이상 트래픽, WAF 탐지 현황 등이 기본 제공되어 설정 즉시 대시보드 확인이 가능합니다.
  * **상관관계 분석(Correlation Rule)**: 여러 로그 소스를 조합하여 복합적인 공격 패턴을 탐지하는 보안 룰 엔진이 내장되어 있습니다.
  * **비용 효율성**: 서드파티 상용 SIEM(Splunk 등)에 비해 데이터 처리량에 비례하는 라이선스 비용이 없어 매우 저렴합니다.
* **단점**: OpenSearch 클러스터(인스턴스 크기, 스토리지 용량)를 사용자가 관리해야 하므로 약간의 운영 공수가 필요합니다.

---

### ② Amazon Security Lake
> **"대규모 멀티 계정 및 하이브리드 환경을 위한 보안 데이터 허브"**

조직 내 퍼져 있는 모든 보안 데이터를 단일 AWS 계정의 중앙 S3 버킷에 자동으로 수집, 정리, 관리하는 전용 보안 데이터 레이크 서비스입니다.

* **동작 방식**: OCSF(Open Cybersecurity Schema Framework)라는 업계 표준 스키마를 사용하여 AWS 로그(VPC, Route53, CloudTrail 등)뿐만 아니라 CrowdStrike, Palo Alto Networks 같은 서드파티 보안 로그까지 데이터 포맷을 단일화하여 S3에 저장합니다.
* **장점**:
  * **표준화된 포맷**: 데이터 포맷 변환 작업을 AWS가 자동으로 수행하므로 데이터 엔지니어링 리소스가 크게 절감됩니다.
  * **뛰어난 연동성**: Splunk, Datadog, IBM QRadar 등의 상용 SIEM과 원클릭에 가깝게 연동이 가능합니다.
  * **초대용량 보관 저비용화**: S3 Intelligent-Tiering 등을 활용하여 수 TB/PB 수준의 보안 로그를 매우 저렴하게 장기 보관할 수 있습니다.
* **단점**: 실시간 검색 화면이나 알림(Alerting)을 직접 제공하지 않으므로, 데이터 소비를 위한 별도의 도구(Athena, 외부 SIEM 등)가 결합되어야 합니다.

---

### ③ AWS Security Hub
> **"포괄적인 위협 경보 통합 및 보안 태세 관리(CSPM)"**

AWS 내 각종 보안 서비스들(Amazon GuardDuty, Inspector, Macie, IAM Access Analyzer 등)과 파트너 솔루션의 **보안 조사 결과(Findings)**를 단일 화면으로 모으고 관리하는 제어 센터입니다.

* **동작 방식**: 로그 자체를 수집하지 않고, '가공 및 탐지 완료된 보안 이벤트(Alert)'들만 표준 포맷(ASFF)으로 수집하여 중앙 대시보드로 보여줍니다.
* **장점**:
  * **매우 쉬운 도입**: 몇 번의 클릭만으로 활성화할 수 있어 관리 공수가 0에 가깝습니다.
  * **보안 컴플라이언스 준수**: CIS AWS Foundations Benchmark, PCI-DSS 등 규정 준수 여부를 자동 스캔하여 점수화해 줍니다.
  * **자동 조치(Remediation)**: Amazon EventBridge와 결합하여 특정 위협 탐지 시 보안 그룹 자동 차단 등의 워크플로우 연동이 용이합니다.
* **단점**: 애플리케이션의 세부 디버그 로그나 로우(Raw) 레벨 웹 로그의 키워드 검색 등 정밀 분석용 SIEM 대용으로는 쓸 수 없습니다.

---

### ④ 서버리스 로그 검색 플랫폼 (Amazon S3 + Athena + QuickSight)
> **"고정 서버 비용 없이 쿼리한 만큼만 지불하는 경량 SIEM"**

모든 원시 로그를 S3에 압축 보관하고, 필요할 때만 표준 SQL을 사용하는 Athena로 직접 쿼리하여 위협을 추적하며, QuickSight를 통해 비정기적인 보고서용 대시보드를 생성하는 서버리스(Serverless) 조합입니다.

* **장점**:
  * **인프라 유지 비용 최저**: 사용자가 프로비저닝할 서버가 전혀 없으므로, 검색 쿼리를 실행할 때 외에는 스토리지 비용만 청구됩니다.
  * **고성능 대용량 쿼리**: Athena의 분산 쿼리 엔진 덕분에 대규모 기가바이트 단위 로그도 수 초 내에 SQL로 조회할 수 있습니다.
* **단점**: 1분 1초를 다투는 실시간 모니터링(실시간 경보 발송 등)을 구현하기에는 적합하지 않고, 정기적인 스캔 비용이 쌓이면 가격 예측이 어려울 수 있습니다.

---

## 2. 상황별 최적의 추천 조합 가이드

| 사용자의 도입 시나리오 | 최적의 추천 아키텍처 | 이유 |
| :--- | :--- | :--- |
| **"로그 비용과 상용 솔루션 비용이 부담스러우나, 대시보드와 실시간 검색 기능은 꼭 필요하다"** | **SIEM on Amazon OpenSearch** | 별도 솔루션 구매 없이 가장 완전한 SIEM 기능(검색, 알림, 대시보드)을 비용 효율적으로 확보할 수 있습니다. |
| **"이미 온프레미스나 타 클라우드에서 Splunk, Datadog 등의 상용 SIEM을 사용 중이다"** | **Amazon Security Lake + 상용 SIEM** | AWS 내부 로그 정규화 과정을 Security Lake에 맡기고, 가공된 깨끗한 데이터를 기존 SIEM으로 가져가 요금과 가용성을 최적화합니다. |
| **"보안 인력이 매우 적어 복잡한 로그 분석 솔루션을 관리할 리소스가 없다"** | **AWS Security Hub (+ GuardDuty)** | 수동 분석 대신 침해 사고 알림 위주로 감시하고, 컴플라이언스 상태만 주 단위로 체크하는 방식으로 공수를 최소화합니다. |
| **"평소에는 대시보드가 필요 없고, 침해 사고 등 이슈가 발생했을 때만 수사(Investigation) 목적으로 분석한다"** | **Amazon S3 + Athena** | 고정 비용을 전혀 들이지 않고 대규모 원시 로그 적재 공간 및 임시 쿼리 환경을 가장 싸게 구축할 수 있는 방법입니다. |
